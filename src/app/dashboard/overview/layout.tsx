// Lumen Health: simple passthrough layout.
// (Replaced the original parallel-route dashboard layout for the patient
// portal use case.)
import React from 'react';

export default function OverviewLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
